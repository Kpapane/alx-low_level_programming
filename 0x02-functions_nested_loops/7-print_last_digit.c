#include "main.h"
/**
 * print_last_digit - prints last digit
 * @n: digit
 * Return: last digit
 */
int print_last_digit(int n)
{
	int a;

	a = (n % 10);

	if (n < 0)
	{
		n = (-1 * a);
	}
	_putchar(x = '0');
	return (x);
}
